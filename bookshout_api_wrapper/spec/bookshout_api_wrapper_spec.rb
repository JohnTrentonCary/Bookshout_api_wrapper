require 'spec_helper'

describe BookshoutApiWrapper do
  it 'has a version number' do
    expect(BookshoutApiWrapper::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
